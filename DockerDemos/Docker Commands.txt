docker version
docker --version
docker info

docker image ls
docker images
docker image ls -q

docker run <image_name>
docker image rm <image_id>

docker container ls
docker container ls -a
docker container ls --all
docker container ls -q
docker container ls -a -q
docker container ls -aq

docker container stop <container_id>
docker container rm <container_id>

docker container start <container_id>

docker container rm $(docker container stop <container_id>)
docker container rm $(docker container stop $(docker container ls -aq))

docker run busybox
docker run busybox echo "Welcome to busybox"
docker run -it busybox sh

docker pull <image>

docker build -t node-docker-example .
docker run -p 3001:3000 node-docker-example

docker run -p <local_port>:<container_port> node-docker-example

docker compose up
docker compose down
docker compose watch

docker compose up --build --force-recreate

docker compose up -d
docker compose up --detach

docker run -d -p 5000:3000 <image_name>

docker ps

